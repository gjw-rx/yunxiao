# 云端 Agent + VSCode 插件跨会话执行记忆

> 用途：后续每个实现会话先阅读本文件，再只处理一个 change。本文是 explore 阶段的结论快照，不替代具体 OpenSpec change。
>
> 当前仓库：`sdks/yunxiao-agent`
>
> 对标源码：`sdks/yunxiao-agent/docs/opencode源码/opencode`

## 一、总体判断

当前系统已经具备 LangGraph 云端编排、SSE 流式交互、VSCode 本地工具、审批、路径守卫、代码智能、终端和 Git 能力；主要差距不在“工具数量”，而在 Agent harness 的可靠性：

- 云端原先缺少稳定的请求级 Run 身份、幂等准入和恢复索引；LangGraph checkpoint 与 AgentTrace 已分别承担 Graph 状态和观测记录。
- 计算生命周期与 SSE 连接耦合，断线、重启、多实例和重复提交缺少统一恢复语义。
- 插件有本地运行状态，但缺少可恢复 RunStore、sequence replay 和非幂等工具执行日志。
- 授权、文件版本、预算和 UI 时间线还没有统一到 run 级别。

目标不是复制 OpenCode 的技术栈，而是形成“云端编排与记录 + 插件受策略约束的本地执行”。

## 二、已完成的 P0

OpenSpec change `stabilize-plugin-run-lifecycle` 已实现并归档，归档位置：

`openspec/changes/archive/2026-08-04-stabilize-plugin-run-lifecycle/`

已完成内容：

1. `tsconfig.json` 只编译插件源码，排除 vendored OpenCode、文档、coverage、dist、out 和 vsix。
2. SessionManager 引入 manager-lifetime generation，旧 run 的 SSE、工具、timeout、error 和 end 回调不能污染新 run。
3. 本地 run 明确区分 `completed`、`cancelled`、`failed`、`disconnected`，保留旧 `stream_end` 兼容事件。
4. tool-result 按 Content-Type 分流；duplicate JSON 不触发 SSE 正常完成、不重试、不重新执行。
5. `code.edit` 审批完成后重新检查预览基线，检测到用户修改时返回不可重试冲突并清理预览。
6. reset/replacement/cancel 会终止活动工具；审批返回后检查 abort signal；旧工具 warning 经过当前 run guard。
7. 已加入网络断线、重复 ack、旧回调、审批竞态和取消竞态回归测试。

## 三、后续分批实施路线

### P1：云端持久 Run Harness

本节只记录本次会话已经完成的内容，以及明确留给后续会话的内容。

#### 已完成

- 只保留一张轻量 `AgentRun`：`run_id`、`session_id`、`client_request_id`、`status`、`last_checkpoint_id` 和时间字段。
- `run_id` 与 `(session_id, client_request_id)` 有唯一约束；`session_id`、`last_checkpoint_id` 和 Trace 关联均为逻辑关联，不使用数据库外键或级联。
- `AgentTrace` 增加可空 `run_id`、`tool_call_id`，继续复用既有模型回复、工具调用、token 和耗时记录。
- 不新增 `AgentStep`、`AgentToolCall` 或 Graph 状态镜像；LangGraph checkpoint 是 Graph 节点、pending task、interrupt、消息和恢复位置的唯一事实来源。
- 初始 `ainvoke`、初始流式执行及 `Command(resume=...)` 均显式使用 `durability="sync"`。
- 生产 PostgreSQL checkpointer 初始化失败时，invoke/stream/resume 统一 fail-closed；只有显式 `memory` 开发模式允许内存 checkpointer。
- `AgentRun` 增加从 1 开始的 `next_event_sequence`；新增轻量 `AgentRunEvent`，只保存 `run_id`、Run 内 `sequence`、事件类型和 JSON 载荷，不复制 Graph 状态。
- 同一 Run 的事件追加使用 PostgreSQL `SELECT ... FOR UPDATE` 锁定 Run 行，在一个事务内分配 sequence、写入事件和推进游标；禁止使用 `max(sequence) + 1`。
- 初始计算和工具结果续算由应用后台 Task 拥有，SSE 订阅只消费持久 RunEvent；客户端断线不会取消云端计算。
- 稳定 JSON 行协议事件已接入 RunEvent；`tool_call`、`run_status=interrupted` 与 sequence 推进在同一事务提交，避免提前续算竞态。
- v2 Run API 已提供幂等创建、显式 Run 工具续算、状态读取和按 `after_sequence` 重放/订阅；v1 streaming 路由保持旧事件负载并通过 `X-Run-ID` 暴露稳定身份。

#### 已完成的服务边界

- 已新增独立 `RunRepository` / `RunService`：幂等创建或回读、状态转换、checkpoint 指针、`pending/running` 恢复扫描。
- `RunService` 不保存 Graph Step，也不自动推进 Graph；Graph 状态仍由 LangGraph checkpointer 读取。
- 已新增独立 `RunEventRepository` / `RunEventService`：事务内事件追加，以及按 `after_sequence` 排他游标升序读取。
- 已新增 `RunComputeService` / `RunSubscriptionService`：后台任务独立持有计算，订阅按数据库 sequence 补发并追随至终态。
- `InvokeService` 继续负责 LangGraph 与消息历史；计算服务负责把公开协议事件持久化，订阅者不再负责归档。
- v2 命令强制 `client_request_id`，v1 消息命令可选传入；同 thread 分布式互斥和 startup recovery 仍属于后续 change。

#### 已完成的验证

- Run/Trace/持久化 guard 与 LangGraph 调用测试已通过；Ruff 已通过。
- Run 事件 metadata、行锁 SQL、事务回滚、游标读取和存量表迁移定向测试 15 项通过；Run 模块覆盖率 86%，Ruff 与 OpenSpec strict validate 通过。
- 计算/订阅/v1-v2 API 定向回归 55 项通过；Run 模块覆盖率 91%；Python compileall、Ruff 和 OpenSpec strict validate 通过。
- 项目依赖现已包含 `psycopg`；未识别到可安全写入的专用 PostgreSQL 测试库，因此真实多连接行锁并发测试尚未执行。

#### 待后续完成

以下内容本次明确没有实现，新会话不要将其当作已完成能力：

- 工具结果 execution receipt、结果 hash 和非幂等工具 exactly-once 防重。
- `resume lease`、Redis/队列/PostgreSQL advisory lock，以及同一 `thread_id` 的跨实例互斥。
- startup recovery worker 自动推进 `pending/running` Run；当前只提供扫描。
- v1 未显式传 `client_request_id` 时的跨请求幂等；v2 已强制接入。
- 插件 RunStore、sequence gap 检测、workspace cursor 与断线自动重连消费。
- content delta 仍按当前流 chunk 持久化，尚未按时间/大小批处理。
- LangGraph Agent Server 迁移。

### P2：插件恢复与本地执行安全

- `RunStore`：按 session/run 保存事件、状态和 cursor。
- sequence gap 检测、replay、workspaceState cursor。
- 非幂等本地工具 execution journal，支持 `unknown` 状态，禁止盲目重放。
- scoped approval：workspace、tool、资源模式、有效期。
- `code.edit` 的文件版本贯穿 read → preview → apply。
- 范围读取、cursor、搜索结果和终端输出 artifact 化。
- 结构化 argv 命令满足策略时自动放行。

P2 依赖 P1 的稳定 RunEvent/sequence/resume 协议；不要在 P1 完成前自行设计一套插件私有 replay 协议。

### P3：预算与 UI

- run 级 step、wall-clock、token、输出大小和重复调用预算。
- 文本 delta 按 100–250ms 或 4KB 批量持久化，避免逐 token 写数据库。
- Webview 时间线只消费 RunStore，不自行推断运行状态。
- 最后再做可访问性、真实 VSCode E2E 和性能基准。

## 四、推荐的独立会话拆分

每个新 session 只选择一个 change：

1. `add-agent-run-persistence`：轻量 AgentRun、AgentTrace 逻辑关联、checkpoint authority、同步 durability 和生产 fail-closed（已完成并归档）。
2. `add-run-event-sequencer`：Run 行锁、sequence、事件写入事务（已归档）。
3. `separate-run-compute-from-subscription`：RunService、StreamProcessor、v1/v2 API（已实施并归档）。
4. `add-run-idempotency-and-recovery`：client request 幂等、tool result hash、lease、startup recovery。（已经实施并归档）
5. `add-plugin-run-store-replay`：插件 RunStore、gap/replay、workspace cursor。
6. `add-local-tool-execution-journal`：非幂等工具 journal 和 unknown 状态。
7. `add-scoped-approval-and-file-versioning`：范围授权和 code.edit 版本链。
8. `add-run-budgets-and-timeline`：预算、delta batch、RunStore 时间线。

### 本次会话实现记录（2026-08-04）

- FastAPI 实现提交：`42b0dcd feat(run):对话运行添加状态参数`。
- 新增 `app/api/agent/run/` 的模型、仓储和 RunService；注册 `agent_run` metadata。
- AgentTrace 增加 `run_id` / `tool_call_id` nullable 逻辑关联，并补充存量表迁移。
- invoke、stream、resume 和内部 reconnect 状态读取使用 LangGraph checkpoint；持久 Graph 调用统一 `durability="sync"`。
- 生产 checkpointer 缺失时拒绝执行，Store 可独立降级；未新增 Step/ToolCall/Event 表。
- 定向测试 20 项通过，Ruff 通过；完整 backend 回归受本地缺少 `psycopg` 依赖阻塞。

### 本次会话实现记录（2026-08-04，RunEvent sequence）

- OpenSpec change：`add-run-event-sequencer`，已生成 proposal、design、spec 和 tasks，当前实现任务已完成并归档。
- `AgentRun.next_event_sequence` 与 `AgentRunEvent` 已加入共享 metadata；启动兼容迁移会为存量 `agent_run` 幂等补列。
- `RunEventService.append` 在独立事务中锁定 Run 行、写入事件并推进游标；失败时事务回滚且不提交游标。
- `RunEventService.list_after` 支持按 Run、排他 sequence cursor、升序和正数 limit 读取。
- 验证命令：`uv run pytest tests/api/agent/run tests/infrastructure/db/test_engine_migrations.py -q`（15 passed）；覆盖率 86%；Ruff 与 `openspec validate add-run-event-sequencer --strict` 通过。
- 扩大回归共 47 passed / 12 failed；失败集中在未修改的旧 Trace 测试与 embedding 参数断言，属于既有测试/API 漂移，不在本 change 修复范围。
- 未执行真实 PostgreSQL 多连接并发测试：当前配置存在 PostgreSQL 连接，但未确认它是可安全写入的专用测试库。
- 下一 session 前置：处理 `add-run-idempotency-and-recovery`，实现持久 client/tool receipt、lease、跨实例互斥和 startup recovery。

### 本次会话实现记录（2026-08-04，计算与订阅解耦）

- OpenSpec change：`separate-run-compute-from-subscription`；proposal、design、3 份 capability spec 与 tasks 已生成，13 项实现任务已完成，并已归档到 `openspec/changes/archive/2026-08-04-separate-run-compute-from-subscription/`。
- 新增 `RunComputeService`：初始消息和工具结果续算使用独立后台 Task；任务只捕获不可变 `run_id/session_id`，不持有请求级 SQLAlchemy session。
- 新增 `RunSubscriptionService`：按排他 sequence 游标补发数据库事件；v2 订阅跨 `interrupted` 保持连接，v1 适配器在当前轮 `interrupted` 或终态追平后结束。
- `RunEventService.append_status` 会在一个 Run 行锁事务中提交状态、`run_status` 事件和游标；`tool_call + interrupted` 也原子提交并使用连续 sequence。
- 新增 `/api/v2/agent/run` 创建、状态、工具结果续算和事件 SSE；v1 `/message/stream` 与 `/tool_result` 保持旧 JSON 行负载并返回 `X-Run-ID`。
- v2 强制 `client_request_id`；v1 可选。重复准入不重复归档用户消息或启动 Graph；续算准入在启动后台任务前写入现有进程内工具结果幂等记录。
- 专项代码审查修复了 v1 默认游标重放历史、续算状态/幂等竞态、无效 Run 在 SSE 200 后才失败，以及请求 ORM 被 rollback 后由后台任务访问的问题。
- 验证：`uv run pytest tests/api/agent/run tests/api/agent/test_invoke_tool_result.py tests/api/agent/test_invoke_service.py tests/api/agent/test_durable_orchestration.py tests/api/agent/test_resume_continuation.py --cov=app.api.agent.run -q` 为 55 passed，Run 模块覆盖率 91%；Python compileall、Ruff 和 `openspec validate separate-run-compute-from-subscription --strict` 通过。
- 扩大到 `tests/api/agent` 为 505 passed / 18 failed / 3 skipped；失败集中在未修改的旧 Trace 内存 API 断言和未提供 checkpointer 的旧 fail-closed 测试，属于既有测试漂移。
- 未执行真实 PostgreSQL 多连接并发测试，也未安装项目外的 pyright；当前类型证据为完整函数注解、compileall、Ruff 与定向测试。
- 未解决风险：后台任务仍是单实例进程内所有权；进程退出会留下 `pending/running` 候选；没有 resume lease、startup recovery worker、持久 execution receipt/result hash 或跨实例 exactly-once。
- 下一 session 前置：处理 `add-run-idempotency-and-recovery`，实现持久 client/tool receipt、lease、跨实例互斥和 startup recovery。

如果一个 change 同时需要数据库迁移、云端 API、插件状态和 Webview，就继续拆分。

## 五、每个新会话的启动模板

开始实现前固定回答：

1. 本 session 只负责哪个 change？
2. 明确修改哪些目录，明确不修改哪些目录？
3. 依赖哪个已完成 change 或协议？
4. RED 测试是什么？
5. 完成标准是什么：类型、Lint、单测、集成测试、迁移检查或 E2E？
6. 是否需要新增/更新 OpenSpec？

结束时记录：变更文件、验证命令、未解决风险、下一 session 的前置条件。默认不要自动 commit，除非用户明确要求。

## 六、明确不应提前做的事

- 不要先做 Webview 大改造，再补 Run 事实来源。
- 不要把持久 Run、事件序列和恢复逻辑继续塞进 `InvokeService`。
- 不要用 `max(sequence)+1` 生成事件序号。
- 不要把 duplicate ack 当成正常 SSE 完成。
- 不要让插件在没有云端 replay/lease 语义时自行猜测运行已经完成。
- 不要把 OpenCode vendored 源码直接纳入插件 TypeScript 编译或运行时。

### 本次会话归档记录（2026-08-04）

- OpenSpec change `add-run-idempotency-and-recovery` 已归档至 `openspec/changes/archive/2026-08-04-add-run-idempotency-and-recovery/`。
- 已沉淀 proposal、design、双 capability spec 与 tasks；归档时跳过主规格同步，因为任务清单仍有未完成项。
- 本次实现并验证：client request 持久化准入、tool result SHA-256 回执与冲突拒绝、Run 初始输入/租约字段、启动扫描及安全恢复分支；定向测试 17 项通过，OpenSpec strict validate 通过。
- 未完成风险：续算路径尚未完整实现跨实例 lease 的续期/释放与完整 exactly-once 保障；启动恢复没有可靠 checkpoint/tool receipt 时禁止盲目重放。后续应新建 change 补齐并增加真实 PostgreSQL 并发测试。

### 本次会话实现记录（2026-08-04，本地工具执行 journal）

- OpenSpec change：`add-local-tool-execution-journal`；已生成 proposal、design、双 capability spec 与 tasks，所有任务已完成，尚未归档。
- 新增 `ToolExecutionJournal`，在 VS Code `workspaceState` 中按 `runId + callId`（旧 v1 流降级为 `sessionId + callId`）保存非只读工具的最小回执；不持久化工具参数。
- `write`、`execute`、`destructive` 工具在审批与安全审计通过后、实际执行前写入 `started`；终态回执复用既有 ToolResult，遗留 `started` 返回 `metadata.execution_state=unknown` 且 `retryable=false`，绝不自动重放。
- 取消或超时后的迟到工具结果不写入终态回执，保持 `started`，以避免副作用结果不确定时误标成功。
- 验证：`npm run check-types`、`npm run lint`、`npm test -- --grep "ToolExecutionJournal|ToolRouter approval gating|SessionManager"`（28 passing）以及 `openspec validate add-local-tool-execution-journal --strict` 均通过。
- 未完成风险：journal 仅是插件工作区级本地防重；尚无云端持久 execution receipt、跨窗口同步、执行 lease 或人工处置 unknown 的 UI。
