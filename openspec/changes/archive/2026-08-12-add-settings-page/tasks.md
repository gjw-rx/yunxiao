## 1. 页面状态与入口

- [x] 1.1 在 `src/webview-ui/App.tsx` 增加聊天页/设置页的本地视图状态，并确保切换不重置现有聊天 reducer、会话或输入状态。
- [x] 1.2 扩展 `SessionHeader`，在新建会话和历史会话按钮之间加入带可访问名称与提示文本的设置按钮，并将进入设置页的回调传入。
- [x] 1.3 实现从设置页返回聊天页的显式操作，确认该流程不新增 Webview-host 消息也不触发现有历史会话请求。

## 2. 设置页面界面

- [x] 2.1 新增设置页 React 组件，提供深色双栏结构、分类导航和默认“模型”分类。
- [x] 2.2 实现“模型”“Skill”“使用情况”三个静态内容面板，明确模型/Skill 不会保存或生效、使用情况不是实时统计。
- [x] 2.3 为设置页补充主题化 CSS：分类选中态、内容卡片/行项目、内容滚动和窄 Webview 可访问性；复用现有样式 token，不新增依赖。

## 3. 测试与验证

- [x] 3.1 为页面切换和设置分类切换补充或更新 Webview 前端测试，覆盖返回后聊天状态保持及无宿主配置消息的边界。
- [x] 3.2 为会话头部按钮顺序及历史下拉保持可用补充或更新组件/行为测试。
- [x] 3.3 执行 `npm run compile`，修复由本变更引入的类型、lint 或打包问题。
