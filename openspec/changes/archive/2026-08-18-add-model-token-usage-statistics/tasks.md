## 1. 模型归属落账

- [x] 1.1 扩展 `TokenUsageSnapshot` 及 Webview/存储相关只读类型，加入非敏感的 provider、模型标识与展示名可选字段，保持旧归档兼容；为新增类型和字段补齐中文注释
- [x] 1.2 在 AgentLoop 构造每步 token 快照时写入本次运行已捕获的模型元数据，并在入口、成功与异常路径通过统一 logger 输出可定位日志，不记录 Secret
- [x] 1.3 增加 AgentLoop/消息持久化测试，验证运行中切换默认模型不会改变当前账归属、下一次调用使用新模型，且序列化结果不含鉴权信息

## 2. 本地用量统计服务

- [x] 2.1 为 SessionFileStore 增加受控的归档 token 记录遍历能力，返回 entry 时间戳与 assistant tokenUsage；确保追加式历史中的每个已落账调用只返回一次并为所有函数补齐中文 JSDoc
- [x] 2.2 新增用量统计模块及只读结果类型，实现本机时区自然日、周一自然周、自然月的左闭右开边界计算，以及按 provider+model 聚合 total/prompt/completion/reasoning/cache 明细
- [x] 2.3 实现旧记录“未知模型”、缺 tokenUsage 忽略、单归档损坏时继续并标记 partial 的兼容逻辑；记录会话/文件、扫描数量、账目数量、耗时与失败原因日志
- [x] 2.4 增加统计单元测试，覆盖多模型求和、缓存不重复计入 total、日/周/月边界、回滚记录仍计入、未知模型、无 token 旧消息及局部归档损坏

## 3. 设置面板协议与宿主处理

- [x] 3.1 在设置 Webview 双向协议中增加 usage 粒度、参考日期、统计快照、partial 与有界错误消息类型，并为全部新增接口、字段和消息补齐中文注释
- [x] 3.2 将统计服务注入设置面板依赖，处理按需 usage 请求并回传标准化区间；在请求入口、成功、失败分支记录粒度、区间、模型数、耗时等中文日志
- [x] 3.3 增加宿主消息处理测试，验证合法日/周/月请求、无数据响应、partial 响应和整体失败不会影响其他设置消息

## 4. 使用情况界面

- [x] 4.1 将 `UsageSettings` 静态占位改为受控视图：首次进入请求当前自然日，提供日/周/月切换及前后周期导航，并保持 usage 状态与其他设置分类草稿隔离
- [x] 4.2 使用现有 VS Code 主题样式实现区间说明、total/prompt/completion 摘要、按 total 降序的模型明细表，以及 loading、空数据、partial、错误重试状态；窄窗口保持可滚动和可访问
- [x] 4.3 更新设置页组件测试，覆盖首次进入才请求、粒度/周期切换、模型行与未知模型展示、空/partial/错误状态，以及模型、Skill、MCP、Hooks 草稿不被 usage 响应清空

## 5. 验证

- [x] 5.1 运行受影响的 Node 与 Webview 测试，修复回归并确认旧 token 归档仍可读取
- [x] 5.2 运行 `npm run compile`，确认 TypeScript strict、ESLint 与 esbuild 全部通过
- [ ] 5.3 手工在 VS Code 设置标签验证日/周/月切换、相邻周期、多个模型、未知模型、空数据与错误提示，并检查 Output Channel 关键日志不含敏感值
