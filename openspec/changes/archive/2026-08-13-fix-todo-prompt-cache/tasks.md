## 1. 压缩检查点任务上下文

- [x] 1.1 扩展压缩检查点消息契约与持久化读写，支持可选的活跃任务上下文，并保持旧 JSONL 记录兼容。
- [x] 1.2 在成功创建检查点时捕获当前活跃任务上下文；在有效历史重建时将其与摘要稳定地提供给模型，并补齐关键路径日志。
- [x] 1.3 为检查点任务上下文的写入、历史重建、空活跃任务和旧检查点兼容性添加单元测试。

## 2. AgentLoop 请求组装

- [x] 2.1 实现模型可见任务状态检测：识别有效历史中匹配当前快照的 `todo_write` 工具结果和当前检查点任务上下文。
- [x] 2.2 移除正常请求的无条件 todo system 注入；仅在活跃任务缺少模型可见证据时注入恢复上下文，并记录会话、决策、任务数量与恢复原因。
- [x] 2.3 确保恢复上下文仍进入请求 token 估算和压缩判定，且不写入 MessageStore 或 Webview 聊天时间线。

## 3. 回归验证

- [x] 3.1 为连续 `todo_write` 更新添加 AgentLoop 测试，验证后续请求通过工具结果获取任务状态且历史前缀不重复插入可变 todo system 消息。
- [x] 3.2 为压缩后恢复和缺失模型可见任务状态的遗留会话添加 AgentLoop 集成测试。
- [x] 3.3 运行相关单元测试与 `npm run compile`，确认类型检查、lint 和打包通过。
