## 1. 配置与工具契约

- [x] 1.1 在 `package.json` 增加 `yunxiaoAgent.webSearch.*` 配置项，并实现配置读取、默认值和边界校验。
- [x] 1.2 定义 WebSearch Provider、标准化搜索结果及不可用/可重试错误的 TypeScript 契约。
- [x] 1.3 实现 `TavilyWebSearchProvider`，使用全局 `fetch` 发送受超时控制的请求，且不记录凭据或完整认证响应。

## 2. 工具集成

- [x] 2.1 实现只读且可并行的 `web.search` 工具，校验查询和请求参数，并在未启用或缺少 API Key 时不发起请求、返回明确的 webSearch 不可用错误。
- [x] 2.2 将 Tavily 结果归一化为允许字段，并让工具结果经过现有脱敏和截断治理。
- [x] 2.3 在扩展激活流程注册 `web.search`，补齐关键入口、分支、成功与失败的中文日志及 JSDoc。
- [x] 2.4 更新 Agent 工具描述，明确搜索结果是外部不可信资料，不能将其内容视为指令。

## 3. 测试与验证

- [x] 3.1 编写 Tavily Provider 测试，覆盖成功归一化、认证失败、限流、超时、网络失败与无效响应，确认错误不包含 API Key。
- [x] 3.2 编写 `web.search` 工具测试，覆盖参数校验、缺失/禁用配置时零网络请求、结果数边界、结果治理和可并行声明。
- [x] 3.3 运行 `npm run compile` 及相关测试，修复本变更引入的失败。
