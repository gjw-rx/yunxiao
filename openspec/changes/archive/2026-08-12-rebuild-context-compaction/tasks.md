## 1. 调研与回归护栏

- [x] 1.1 确认 Hermes 调研文档、proposal、design 和 delta specs 已覆盖预算、检查点和工具配对不变量
- [x] 1.2 先编写并运行压缩预算、增量检查点、工具孤儿清理和摘要失败的单元测试，确认 RED

## 2. 压缩内核

- [x] 2.1 重建 `compaction.ts`：完整请求阈值、增量摘要、尾部工具链原子保留、孤立工具对清理和结构化结果
- [x] 2.2 扩展 MessageStore/HistoryLoader 以提供最新检查点代表的有效压缩上下文，且保持 append-only 历史
- [x] 2.3 调整 AgentLoop 在完整请求组装后预检自动/溢出压缩，复用 tokenEstimator 且避免 system prompt 重复计数

## 3. 配置与手动入口

- [x] 3.1 在模型档案、校验、宿主协议和模型设置页增加最大上下文 token，默认 262144，并在模型切换时同步
- [x] 3.2 在 package.json 定义原生 VSCode 自动压缩设置，监听设置变更；停用旧固定压缩参数
- [x] 3.3 接入 `/compact` 菜单、精确文本命令和宿主执行路径，运行中拒绝且不写入消息历史

## 4. 验证与可观测性

- [x] 4.1 为模型最大上下文、原生设置和 `/compact` 补齐单元/组件测试，并验证日志覆盖关键分支
- [x] 4.2 运行相关测试、`npm run compile` 与 OpenSpec 校验，修复发现的问题
- [x] 4.3 修复手动压缩受自动尾部预算阻断的问题，并覆盖多条短历史的强制压缩回归用例
- [x] 4.4 为摘要请求预留推理与正文的输出预算，并覆盖仅推理响应的回归用例
