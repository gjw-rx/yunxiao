## 1. 审批模式契约与持久化

- [x] 1.1 在 `package.json` 和审批核心中定义工作区级 `request` / `full-access` 审批模式配置、默认值与中文说明。
- [x] 1.2 为 `ApprovalGateway` 增加可注入的审批模式存储和读取接口；缺失或非法配置必须回退到 `request`。
- [x] 1.3 补齐模式读取、写入、非法值回退与关键分支的中文日志及 JSDoc。

## 2. 核心审批与终端删除保护

- [x] 2.1 在 `ApprovalGateway` 中实现完全访问对非删除操作的无提示 `allow`，确保不创建 session/scoped grant，并保持关闭模式后的现有优先级和行为。
- [x] 2.2 确保 destructive 删除工具在完全访问模式下仍执行现有人工审批与二次确认流程。
- [x] 2.3 在 `ShellWhitelist` 或同一终端安全分类模块中实现可测试的删除命令识别，覆盖 `rm`、`rmdir`、`del`/`erase`、`Remove-Item` 和 `git clean`。
- [x] 2.4 更新 `TerminalExecTool`：未被硬安全规则拦截的删除命令走 destructive 审批，其他未知非删除命令可由完全访问模式自动批准；保留路径守卫、安全审计和危险命令拦截顺序。

## 3. 宿主与输入区交互

- [x] 3.1 扩展 Webview 双向协议、状态和 reducer，支持宿主下发当前审批模式及 Webview 提交模式切换请求。
- [x] 3.2 在 `ChatPanel` 的握手与消息处理链路中读取、校验、持久化并广播审批模式，记录成功和无效请求日志。
- [x] 3.3 在 `MessageInput` 的“+”右侧实现审批模式按钮和两项菜单，提供当前状态、完整中文风险说明、键盘 Escape 和点击外部关闭行为。
- [x] 3.4 在 `chat.css` 添加最小样式，遵循现有工具栏、主题变量、焦点态和窄宽度布局。

## 4. 验证

- [x] 4.1 为 `ApprovalGateway` 新增完全访问自动批准、删除不绕过、关闭后恢复常规审批、持久化回退和日志分支测试。
- [x] 4.2 为终端分类与执行新增跨 shell 删除命令、完全访问的非删除命令、危险命令优先拦截测试。
- [x] 4.3 为 Webview 协议、reducer 和输入菜单新增模式同步、菜单可访问性和模式切换测试。
- [x] 4.4 运行 `npm run compile` 及相关测试套件，修复本变更引入的类型、lint 或行为失败。
